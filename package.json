{
  "name": "crs-dispatch",
  "version": "0.1.0",
  "private": true,
  "description": "CRS field work dispatch board — Express API + React board over Salesforce",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}